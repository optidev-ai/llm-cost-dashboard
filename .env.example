# Mode ② — point the "Connect your key" dialog at your usage edge function.
# Base URL of the deployed function (the dialog POSTs to ${VITE_USAGE_PROXY_URL}/usage).
# Leave unset to run in demo mode (seeded data).
# VITE_USAGE_PROXY_URL=https://<your-project>.supabase.co/functions/v1
